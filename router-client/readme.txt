To run the client: python repy.py restrictions.default dylink.r2py client.r2py

To run the router: python repy.py restrictions.default router.r2py

In router code, LENGTH is the packet length, WIN is the window length where the router checks for acks and losses, DEBUG = 1 or 0 to turn verbose output on and off.
